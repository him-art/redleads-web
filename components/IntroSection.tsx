'use client';

export default function IntroSection() {
  return (
    <section className="relative w-full h-full bg-[#1a1a1a] text-white flex items-center justify-center">
      <div className="text-center space-y-8 animate-fade-in">
        {/* Minimal content as requested - just a bridge */}
      </div>
    </section>
  );
}
